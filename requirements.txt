# BBC College AI Chatbot — Python dependencies
# Compatible with Python 3.10, 3.11, 3.12, 3.13 on Windows / Mac / Linux
# Install: pip install -r requirements.txt

# ── Core Web Framework ──────────────────────────────────────────
flask>=3.0.3
flask-login>=0.6.3
flask-session>=0.8.0
python-dotenv>=1.0.1

# ── AI / OpenAI ─────────────────────────────────────────────────
openai>=1.30.1

# ── Semantic Search (sentence-transformers + numpy) ─────────────
# numpy 2.x ships pre-built wheels for Python 3.13 on Windows.
# Do NOT pin to 1.26.4 — it requires a C compiler on Py 3.13.
numpy>=2.0.0
scikit-learn>=1.5.0
sentence-transformers>=3.0.1

# ── Security ────────────────────────────────────────────────────
bcrypt>=4.1.3

# ── PDF Export ──────────────────────────────────────────────────
fpdf2>=2.7.9

# ── Production WSGI (Linux / Mac only — skip on Windows) ────────
# gunicorn does NOT run on Windows. Use waitress instead.
# On Windows:  pip install waitress
# On Linux/Mac: gunicorn is installed automatically via this line.
gunicorn>=22.0.0; sys_platform != "win32"
waitress>=3.0.0; sys_platform == "win32"

# ── Testing ─────────────────────────────────────────────────────
pytest>=8.0.0
